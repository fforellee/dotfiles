- name: setup system
  hosts: localhost
  tasks:
  - name: create directory tree
    file:
        path: ~/{teste,testee,testee}
        state: directory

  - name: install packages
    when: ansible_facts['distribution'] == "Archlinux"
    community.general.pacman:
      name:
        -alacritty
      state: present
      
